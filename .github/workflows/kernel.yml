name: Build Kernel

on:
  workflow_dispatch:

jobs:
  build:
    name: "构建内核 v7.1.0"
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: write

    steps:
      - name: "📥 检出代码"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: "💾 设置 ccache 编译缓存"
        uses: actions/cache@v4
        id: cache-ccache
        with:
          path: /home/runner/.ccache
          key: ${{ runner.os }}-ccache-unified-kernel-7.1.0-${{ github.ref_name }}
          restore-keys: |
            ${{ runner.os }}-ccache-unified-kernel-7.1.0-
            ${{ runner.os }}-ccache-unified-kernel-

      - name: "🔧 部署 LLVM 交叉编译环境"
        run: |
          echo "⚙️ 正在安装必要的构建工具..."
          sudo apt update
          sudo apt install -y build-essential flex bison libssl-dev libelf-dev bc ccache clang llvm lld wget xz-utils unzip autoconf automake libtool libyaml-dev libbsd-dev
          echo "✅ 构建工具安装完成"

          mkdir -p /home/runner/.ccache
          ccache --set-config=max_size=10G
          ccache --set-config=cache_dir=/home/runner/.ccache
          ccache --set-config=sloppiness=file_macro,locale,time_macros
          ccache --set-config=hash_dir=false

      - name: "🔨 编译内核"
        id: build
        run: |
          echo "🚀 开始编译内核版本 7.1.0"

          export CCACHE_DIR=/home/runner/.ccache
          export CCACHE_MAXSIZE=10G

          # ==========================================
          # 所有的卡死修复和报错剔除都已经写在脚本里了
          # 这里直接无脑运行即可！
          # ==========================================
          chmod +x sheng-kernel_build.sh
          bash sheng-kernel_build.sh

          echo "✅ 内核编译完成"
          ccache --show-stats || true

      - name: "📦 收集并统计编译产物"
        id: collect_artifacts
        run: |
          PKG_COUNT=$(ls -1 *.deb 2>/dev/null | wc -l)
          echo "📦 共找到 $PKG_COUNT 个 Debian 包文件"
          echo "pkg_count=$PKG_COUNT" >> $GITHUB_OUTPUT

      - name: "📤 上传生成的配置文件"
        uses: actions/upload-artifact@v4
        with:
          name: generated-config
          path: sm8550.config

      - name: "📤 上传系统驱动包 (.deb)"
        uses: actions/upload-artifact@v4
        with:
          name: unified-kernel-debs-7.1.0
          path: "*.deb"
          retention-days: 7
          if-no-files-found: error

      - name: "📤 上传启动镜像 (boot.img)"
        uses: actions/upload-artifact@v4
        with:
          name: boot_images
          path: |
            boot_sheng_singleboot.img
            boot_sheng_dualboot.img

      - name: "🏷️ 自动发布 GitHub Release"
        if: steps.collect_artifacts.outputs.pkg_count > 0 && github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: kernel-7.1.0
          name: "Kernel v7.1.0"
          body: |
            **小米Pad6spro通用内核 v7.1.0**

          draft: false
          prerelease: false
          files: |
            *.deb
            *.img
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

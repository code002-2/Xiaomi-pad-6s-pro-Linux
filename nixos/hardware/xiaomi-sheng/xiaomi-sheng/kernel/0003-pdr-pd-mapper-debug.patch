diff --git a/drivers/soc/qcom/pdr_interface.c b/drivers/soc/qcom/pdr_interface.c
index 72259f4..8b661ba 100644
--- a/drivers/soc/qcom/pdr_interface.c
+++ b/drivers/soc/qcom/pdr_interface.c
@@ -85,6 +85,8 @@ static int pdr_locator_new_server(struct qmi_handle *qmi,
 	pdr->locator_init_complete = true;
 	mutex_unlock(&pdr->lock);
 
+	pr_debug("PDR: locator new server node=%d port=%d\n", svc->node, svc->port);
+
 	/* Service pending lookup requests */
 	schedule_work(&pdr->locator_work);
 
@@ -386,6 +388,9 @@ static int pdr_get_domain_list(struct servreg_get_domain_list_req *req,
 		return -EREMOTEIO;
 	}
 
+	pr_debug("PDR: %s get domain list returned %d domains (of %d total)\n",
+		req->service_name, resp->domain_list_len, resp->total_domains);
+
 	return 0;
 }
 
@@ -414,6 +419,8 @@ static int pdr_locate_service(struct pdr_handle *pdr, struct pdr_service *pds)
 		for (i = 0; i < resp->domain_list_len; i++) {
 			entry = &resp->domain_list[i];
 
+			pr_debug("PDR: domain candidate: name='%s' instance=%u\n", entry->name, entry->instance);
+
 			if (strnlen(entry->name, sizeof(entry->name)) == sizeof(entry->name))
 				continue;
 
@@ -435,6 +442,8 @@ static int pdr_locate_service(struct pdr_handle *pdr, struct pdr_service *pds)
 		domains_read += resp->domain_list_len;
 	} while (domains_read < resp->total_domains);
 
+	pr_err("PDR: %s service '%s' not found in %d domains\n", pds->service_name, pds->service_path, domains_read);
+
 	return ret;
 }
 
@@ -540,6 +549,9 @@ struct pdr_service *pdr_add_lookup(struct pdr_handle *pdr,
 	list_add(&pds->node, &pdr->lookups);
 	mutex_unlock(&pdr->list_lock);
 
+	pr_debug("PDR: adding lookup for service_name=%s service_path=%s\n",
+		service_name, service_path);
+
 	schedule_work(&pdr->locator_work);
 
 	return_ptr(pds);
diff --git a/drivers/soc/qcom/pmic_glink.c b/drivers/soc/qcom/pmic_glink.c
index 627f96c..98d334f 100644
--- a/drivers/soc/qcom/pmic_glink.c
+++ b/drivers/soc/qcom/pmic_glink.c
@@ -231,6 +231,8 @@ static void pmic_glink_pdr_callback(int state, char *svc_path, void *priv)
 {
 	struct pmic_glink *pg = priv;
 
+	dev_dbg(pg->dev, "pmic_glink_pdr_callback: svc_path=%s state=%d\n", svc_path, state);
+
 	guard(mutex)(&pg->state_lock);
 	pg->pdr_state = state;
 
@@ -249,6 +251,8 @@ static int pmic_glink_rpmsg_probe(struct rpmsg_device *rpdev)
 	dev_set_drvdata(&rpdev->dev, pg);
 	pg->pdr_available = rpdev->id.driver_data;
 
+	dev_dbg(&rpdev->dev, "pmic_glink_rpmsg_probe: pg->pdr_available=%d\n", pg->pdr_available);
+
 	guard(mutex)(&pg->state_lock);
 	pg->ept = rpdev->ept;
 	if (!pg->pdr_available)
@@ -338,12 +342,14 @@ static int pmic_glink_probe(struct platform_device *pdev)
 			goto out_release_altmode_aux;
 	}
 
+	dev_dbg(&pdev->dev, "pmic_glink_probe: calling pdr_add_lookup for tms/servreg, msm/adsp/charger_pd\n");
 	service = pdr_add_lookup(pg->pdr, "tms/servreg", "msm/adsp/charger_pd");
 	if (IS_ERR(service)) {
 		ret = dev_err_probe(&pdev->dev, PTR_ERR(service),
 				    "failed adding pdr lookup for charger_pd\n");
 		goto out_release_aux_devices;
 	}
+	dev_dbg(&pdev->dev, "pmic_glink_probe: pdr_add_lookup successful\n");
 
 	mutex_lock(&__pmic_glink_lock);
 	__pmic_glink = pg;
diff --git a/drivers/soc/qcom/qcom_pd_mapper.c b/drivers/soc/qcom/qcom_pd_mapper.c
index dc10bc8..d7a06c8 100644
--- a/drivers/soc/qcom/qcom_pd_mapper.c
+++ b/drivers/soc/qcom/qcom_pd_mapper.c
@@ -198,7 +198,7 @@ static void qcom_pdm_get_domain_list(struct qmi_handle *qmi,
 		}
 	}
 
-	pr_debug("PDM: service '%s' offset %d returning %d domains (of %d)\n", req->service_name,
+	pr_debug("PDM: service '%s' offset %d returning %d domains (of %d)\n", req->service_name,
 		 req->domain_offset_valid ? req->domain_offset : -1, rsp->domain_list_len, rsp->total_domains);
 
 	ret = qmi_send_response(qmi, sq, txn, SERVREG_GET_DOMAIN_LIST_REQ,
@@ -663,6 +663,8 @@ static struct qcom_pdm_data *qcom_pdm_start(void)
 		goto err_stop;
 	}
 
+	pr_debug("PDM: qcom_pdm_start completed successfully\n");
+
 	return data;
 
 err_stop:
